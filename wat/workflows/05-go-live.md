# 05 — Go-Live Workflow

## Goal
Synthesize coding + design + marketing + QA into the eight shipped deliverables, apply QA fixes inline, and commit on `tutorial/02-bolt-post`.

## Constraints
- Deliverables overwrite anything in `deliverables/`. The eight files are the contract.
- `wat-charter.md` is the pipeline transparency log — every sub-agent's role, inputs, outputs, and any deviations from the workflow files.
- Commit message: `tutorial(02-bolt-post): GO — long-form package ready`.
- No `git push`. Local commit only.

## Done when
- All 8 files exist in `deliverables/`.
- `git status` is clean post-commit.
- Final return message lists title, length estimate, commit SHA, blockers, confirmation.
